import { User } from "./User";

export class Customization {
    constructor(public avatar: string, public theme: string) {}
  
    saveCustomization(user: User): void {
      // Implementação futura
    }
  }
  